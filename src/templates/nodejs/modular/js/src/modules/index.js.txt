const { tryProductRoutes } = require("./products/product.routes");

function dispatchModules(req, res) {
  if (tryProductRoutes(req, res)) return;
  res.writeHead(404, { "Content-Type": "text/plain; charset=utf-8" });
  res.end("Not found");
}

module.exports = { dispatchModules };
